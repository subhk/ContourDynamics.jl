import{_ as a,o as i,c as s,an as l}from"./chunks/framework.BZIBkoIN.js";const u=JSON.parse('{"title":"Architecture Guide","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md","lastUpdated":null}'),n={name:"architecture.md"};function o(t,e,r,c,p,d){return i(),s("div",null,[...e[0]||(e[0]=[l(`<h1 id="Architecture-Guide" tabindex="-1">Architecture Guide <a class="header-anchor" href="#Architecture-Guide" aria-label="Permalink to &quot;Architecture Guide {#Architecture-Guide}&quot;">​</a></h1><p>This page is the shortest path through the codebase for a new contributor.</p><p>If you only want to understand how a simulation runs, read these files in order:</p><ol><li><p><a href="https://github.com/subhk/ContourDynamics.jl/blob/main/src/core/problem_factory.jl" target="_blank" rel="noreferrer"><code>src/core/problem_factory.jl</code></a></p></li><li><p><a href="https://github.com/subhk/ContourDynamics.jl/blob/main/src/core/evolution.jl" target="_blank" rel="noreferrer"><code>src/core/evolution.jl</code></a></p></li><li><p><a href="https://github.com/subhk/ContourDynamics.jl/blob/main/src/velocity/common.jl" target="_blank" rel="noreferrer"><code>src/velocity/common.jl</code></a></p></li><li><p><a href="https://github.com/subhk/ContourDynamics.jl/blob/main/src/beta_plane.jl" target="_blank" rel="noreferrer"><code>src/beta_plane.jl</code></a>, for beta-plane QG problems</p></li></ol><h2 id="Mental-Model" tabindex="-1">Mental Model <a class="header-anchor" href="#Mental-Model" aria-label="Permalink to &quot;Mental Model {#Mental-Model}&quot;">​</a></h2><p>The package simulates piecewise-constant PV patches by evolving their <strong>contour nodes</strong>.</p><p>The main object flow is:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Problem(...) -&gt; ContourProblem / MultiLayerContourProblem</span></span>
<span class="line"><span>             -&gt; timestep!(...)</span></span>
<span class="line"><span>             -&gt; velocity!(...)</span></span>
<span class="line"><span>             -&gt; contour node updates</span></span>
<span class="line"><span>             -&gt; optional surgery!</span></span></code></pre></div><p>Most code falls into one of six layers:</p><ul><li><p><code>src/core/</code>: types, problem construction, time integration, surgery, geometry helpers</p></li><li><p><code>src/beta_plane.jl</code>: beta-plane QG velocity composition</p></li><li><p><code>src/velocity/</code>: public velocity API and direct CPU policies</p></li><li><p><code>src/velocity/periodic/</code>: Ewald cache and periodic single-layer corrections</p></li><li><p><code>src/accel/ka/</code>: KernelAbstractions-based direct kernels for CPU/GPU backends</p></li><li><p><code>src/diagnostics/</code>: geometry and integral diagnostics</p></li></ul><h2 id="Codebase-Structure" tabindex="-1">Codebase Structure <a class="header-anchor" href="#Codebase-Structure" aria-label="Permalink to &quot;Codebase Structure {#Codebase-Structure}&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>src/</span></span>
<span class="line"><span>├── ContourDynamics.jl            module definition, includes, exports</span></span>
<span class="line"><span>├── beta_plane.jl                 beta-plane QG velocity composition</span></span>
<span class="line"><span>├── core/</span></span>
<span class="line"><span>│   ├── types.jl                  includes the type-definition files below</span></span>
<span class="line"><span>│   ├── kernel_types.jl           EulerKernel, QGKernel, SQGKernel, MultiLayerQGKernel</span></span>
<span class="line"><span>│   ├── beta_plane_types.jl       BetaPlaneQGKernel + the shared sawtooth jet formula</span></span>
<span class="line"><span>│   ├── contour_types.jl          PVContour</span></span>
<span class="line"><span>│   ├── domain_types.jl           UnboundedDomain, PeriodicDomain</span></span>
<span class="line"><span>│   ├── problem_types.jl          ContourProblem, MultiLayerContourProblem</span></span>
<span class="line"><span>│   ├── stepper_types.jl          RK4Stepper, LeapfrogStepper</span></span>
<span class="line"><span>│   ├── surgery_types.jl          SurgeryParams</span></span>
<span class="line"><span>│   ├── problem.jl                the high-level Problem wrapper</span></span>
<span class="line"><span>│   ├── problem_factory.jl        keyword construction of Problem</span></span>
<span class="line"><span>│   ├── contours.jl               contour geometry, remeshing, beta_staircase</span></span>
<span class="line"><span>│   ├── domains.jl                periodic wrapping, minimum-image helpers</span></span>
<span class="line"><span>│   ├── surgery.jl                CPU surgery: filaments, reconnection, remesh</span></span>
<span class="line"><span>│   ├── evolution.jl              evolve!, timestep!, periodic wrap dispatch</span></span>
<span class="line"><span>│   ├── evolution_buffers.jl      flat packing/scatter, stepper update kernels</span></span>
<span class="line"><span>│   ├── device.jl                 CPU/GPU device tags and allocation shims</span></span>
<span class="line"><span>│   ├── device_state.jl           DeviceContourState, the device-resident layout</span></span>
<span class="line"><span>│   ├── shapes.jl                 circular_patch, elliptical_patch, …</span></span>
<span class="line"><span>│   └── show.jl                   pretty printing for the public types</span></span>
<span class="line"><span>├── velocity/</span></span>
<span class="line"><span>│   ├── common.jl                 public velocity! API and dispatch policy</span></span>
<span class="line"><span>│   ├── unbounded/single_layer.jl unbounded Euler/QG/SQG segment velocity</span></span>
<span class="line"><span>│   └── periodic/</span></span>
<span class="line"><span>│       ├── cache.jl              Ewald cache construction and locking</span></span>
<span class="line"><span>│       └── single_layer.jl       periodic single-layer corrections</span></span>
<span class="line"><span>├── accel/ka/</span></span>
<span class="line"><span>│   ├── packing.jl                flat SegmentData layout, reusable workspaces</span></span>
<span class="line"><span>│   ├── kernels.jl                @kernel velocity kernels and scalar helpers</span></span>
<span class="line"><span>│   ├── velocity.jl               launch wrappers, dispatch, entry points</span></span>
<span class="line"><span>│   └── surgery/                  device-resident surgery pipeline</span></span>
<span class="line"><span>│       ├── types.jl              FlatContourTopology and the flat packing helpers</span></span>
<span class="line"><span>│       ├── filaments.jl          filament flagging and stream compaction</span></span>
<span class="line"><span>│       ├── pairs.jl              close-pair detection, admissibility, pair selection</span></span>
<span class="line"><span>│       ├── rewrite.jl            split/merge topology rewrite and output layout</span></span>
<span class="line"><span>│       ├── remesh.jl             Dritschel weighted remeshing, area preservation</span></span>
<span class="line"><span>│       └── driver.jl             pipeline drivers and the public surgery! methods</span></span>
<span class="line"><span>└── diagnostics/</span></span>
<span class="line"><span>    ├── geometry.jl               area, circulation, enstrophy, angular momentum</span></span>
<span class="line"><span>    ├── ka_energy.jl              device-resident energy, single- and multi-layer</span></span>
<span class="line"><span>    ├── unbounded/                unbounded energy, single- and multi-layer</span></span>
<span class="line"><span>    └── periodic/                 periodic energy, single- and multi-layer</span></span>
<span class="line"><span></span></span>
<span class="line"><span>ext/                              package extensions, loaded on demand</span></span>
<span class="line"><span>├── ContourDynamicsCUDAExt.jl     wires GPU() to CuArray and the CUDA KA backend</span></span>
<span class="line"><span>├── ContourDynamicsDiffEqExt.jl   OrdinaryDiffEq bridge (CPU state)</span></span>
<span class="line"><span>├── ContourDynamicsJLD2Ext.jl     checkpointing and recorders</span></span>
<span class="line"><span>├── ContourDynamicsMakieExt.jl    plotting and animation</span></span>
<span class="line"><span>└── ContourDynamicsRecordedArraysExt.jl   time-series recording</span></span></code></pre></div><p>The densest reading is <code>core/surgery.jl</code> (the CPU reference surgery pass) and the <code>accel/ka/surgery/</code> stages that mirror it on the device; neither is a good starting point for reading the package. <code>core/evolution_buffers.jl</code> is likewise mostly flat-buffer bookkeeping rather than model logic.</p><h2 id="Core-Types" tabindex="-1">Core Types <a class="header-anchor" href="#Core-Types" aria-label="Permalink to &quot;Core Types {#Core-Types}&quot;">​</a></h2><p>The object model is split by responsibility under <code>src/core/</code>:</p><ul><li><p><code>kernel_types.jl</code>: <code>EulerKernel</code>, <code>QGKernel</code>, <code>SQGKernel</code>, <code>MultiLayerQGKernel</code></p></li><li><p><code>beta_plane_types.jl</code>: <code>BetaPlaneQGKernel</code></p></li><li><p><code>contour_types.jl</code>: <code>PVContour</code></p></li><li><p><code>domain_types.jl</code>: <code>UnboundedDomain</code>, <code>PeriodicDomain</code></p></li><li><p><code>problem_types.jl</code>: <code>ContourProblem</code>, <code>MultiLayerContourProblem</code></p></li><li><p><code>surgery_types.jl</code>: <code>SurgeryParams</code></p></li><li><p><code>stepper_types.jl</code>: <code>RK4Stepper</code>, <code>LeapfrogStepper</code></p></li></ul><p>The high-level wrapper type is in <code>src/core/problem.jl</code>:</p><ul><li><p><code>Problem</code> bundles a contour problem, a stepper, and optional surgery parameters</p></li><li><p><code>Problem(...)</code> keyword construction is implemented in <code>src/core/problem_factory.jl</code></p></li></ul><h2 id="Execution-Flow" tabindex="-1">Execution Flow <a class="header-anchor" href="#Execution-Flow" aria-label="Permalink to &quot;Execution Flow {#Execution-Flow}&quot;">​</a></h2><h3 id="1.-Build-a-problem" tabindex="-1">1. Build a problem <a class="header-anchor" href="#1.-Build-a-problem" aria-label="Permalink to &quot;1. Build a problem {#1.-Build-a-problem}&quot;">​</a></h3><p>Most users start with:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">prob </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Problem</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(; contours</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">circular_patch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">128</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2pi</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)], dt</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.01</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The constructor in <code>src/core/problem_factory.jl</code> does six things:</p><ol><li><p>validates whether this is single-layer or multilayer</p></li><li><p>normalizes contour or layer precision to the requested <code>T</code></p></li><li><p>builds the kernel and domain</p></li><li><p>attaches beta-plane reference contours when <code>kernel=:beta_plane_qg</code></p></li><li><p>builds <code>ContourProblem</code> or <code>MultiLayerContourProblem</code></p></li><li><p>builds the time stepper and surgery settings</p></li></ol><p>When input contours already have element type <code>T</code>, their containing vectors are reused. Mismatched inputs are copied into <code>PVContour{T}</code> values, including node, wrap, and corner data, so the contour problem and time-stepper buffers always agree on precision.</p><h3 id="2.-Evolve-in-time" tabindex="-1">2. Evolve in time <a class="header-anchor" href="#2.-Evolve-in-time" aria-label="Permalink to &quot;2. Evolve in time {#2.-Evolve-in-time}&quot;">​</a></h3><p><code>evolve!</code> in <code>src/core/evolution.jl</code> is the main simulation loop:</p><ol><li><p>run callbacks for step 0</p></li><li><p>call <code>timestep!</code></p></li><li><p>wrap nodes for periodic domains</p></li><li><p>optionally run <code>surgery!</code></p></li><li><p>resize buffers if surgery changed node counts</p></li><li><p>run callbacks for the new step</p></li></ol><p>The low-level flat-buffer packing/scattering helpers live in <code>src/core/evolution_buffers.jl</code> so the public stepping logic stays readable.</p><h3 id="3.-Compute-velocities" tabindex="-1">3. Compute velocities <a class="header-anchor" href="#3.-Compute-velocities" aria-label="Permalink to &quot;3. Compute velocities {#3.-Compute-velocities}&quot;">​</a></h3><p><code>velocity!</code> in <code>src/velocity/common.jl</code> is the top-level dispatcher.</p><p>For single-layer CPU problems the current policy is:</p><ul><li>direct evaluation, including beta-plane QG</li></ul><p>For multilayer CPU problems:</p><ul><li>direct modal decomposition</li></ul><p>For GPU-tagged problems — single-layer Euler, QG, SQG, and beta-plane QG, and multi-layer QG:</p><ul><li>KernelAbstractions direct path, reading the device-resident state</li></ul><p>All GPU-tagged problems are device-resident: velocity, RK4/leapfrog timestepping, periodic wrapping, surgery, and diagnostics operate on <code>DeviceContourState</code> without a per-step host round-trip. Surgery on unbounded domains runs entirely on the device; periodic surgery materializes at the host boundary, runs the CPU pass, and reloads the device state in place.</p><h2 id="Velocity-Backends" tabindex="-1">Velocity Backends <a class="header-anchor" href="#Velocity-Backends" aria-label="Permalink to &quot;Velocity Backends {#Velocity-Backends}&quot;">​</a></h2><p>The package has several velocity implementations.</p><h3 id="Direct-CPU" tabindex="-1">Direct CPU <a class="header-anchor" href="#Direct-CPU" aria-label="Permalink to &quot;Direct CPU {#Direct-CPU}&quot;">​</a></h3><p>Implemented mostly in <code>src/velocity/common.jl</code>.</p><ul><li><p>easiest to read</p></li><li><p>useful as the reference implementation</p></li><li><p>used for small problems and many tests</p></li><li><p>beta-plane QG adds its direct correction in <code>src/beta_plane.jl</code></p></li></ul><h3 id="Periodic-/-Ewald" tabindex="-1">Periodic / Ewald <a class="header-anchor" href="#Periodic-/-Ewald" aria-label="Permalink to &quot;Periodic / Ewald {#Periodic-/-Ewald}&quot;">​</a></h3><p>Implemented in <code>src/velocity/periodic/</code>.</p><ul><li><p><code>cache.jl</code>: Ewald cache construction and locking</p></li><li><p><code>single_layer.jl</code>: periodic single-layer corrections</p></li></ul><p>For periodic CPU direct velocity, the code prefetches the Ewald cache once per call.</p><h3 id="KernelAbstractions-KA" tabindex="-1">KernelAbstractions (KA) <a class="header-anchor" href="#KernelAbstractions-KA" aria-label="Permalink to &quot;KernelAbstractions (KA) {#KernelAbstractions-KA}&quot;">​</a></h3><p>Implemented under <code>src/accel/ka/</code>, split by concern:</p><ul><li><p><code>packing.jl</code>: flat <code>SegmentData</code> layout and per-size workspace buffers</p></li><li><p><code>kernels.jl</code>: scalar contribution helpers and the <code>@kernel</code> velocity kernels</p></li><li><p><code>velocity.jl</code>: launch wrappers, dispatch, and the <code>_ka_velocity!</code> entry points</p></li><li><p><code>surgery/</code>: the staged device-resident surgery pipeline</p></li></ul><p>The KA layer runs on both backends:</p><ul><li><p>CPU execution through <code>KernelAbstractions.CPU()</code> (used to validate the kernels against the scalar reference in tests)</p></li><li><p>GPU execution through the CUDA extension</p></li></ul><p>The KA layer contains:</p><ul><li><p>flat segment kernels</p></li><li><p>periodic KA variants</p></li><li><p>flat contour topology buffers backing the device surgery pipeline</p></li><li><p>device-side contour cleanup flags, close-pair candidate detection, and compact close-pair candidate buffers</p></li><li><p>device-side admissibility filtering for unbounded close-pair buffers, including the same-local-interior-vorticity merge predicate</p></li><li><p>device-side reconnect distance planning with compact selected-pair buffers built by a KA greedy selection kernel over device distances</p></li><li><p>device-side topology rewrite sizing for selected split/merge operations</p></li><li><p>device-side materialization of selected split/merge operation outputs</p></li><li><p>device-side full contour-list layout and prefix-offset construction, followed by materialization that copies unchanged contours, replaces split/merge sources, skips deleted merge targets, and appends split daughters in the same order as the CPU reconnect path</p></li><li><p>device-side Dritschel weighted remeshing for closed contours, including fixed-corner span remeshing after topology surgery</p></li><li><p>an unbounded <code>GPU()</code> surgery dispatch (single-layer, and per-layer for multi-layer problems) that uses device-side cleanup flags, close-pair scans, remeshing, reconnection planning, and contour rewrites, then updates the active <code>DeviceContourState</code></p></li><li><p>a periodic <code>GPU()</code> surgery dispatch using device-side minimum-image admissibility, deterministic pair selection, and cross-seam frame shifts in the topology rewrite kernels</p></li><li><p>multi-layer velocity evaluation through the state-based modal evaluator, which packs per-layer segments with modal PV weights and reuses the single-layer KA kernels once per vertical mode</p></li><li><p>multi-layer energy through the same modal trick: the segment geometry is packed once and only the per-segment PV weight is rewritten per mode</p></li><li><p>the beta-plane device path, which caches the frozen reference staircase with negated PV in the tail of its segment buffers and adds the analytic sawtooth jet with a separate kernel</p></li></ul><h2 id="Threading-and-Parallelism" tabindex="-1">Threading and Parallelism <a class="header-anchor" href="#Threading-and-Parallelism" aria-label="Permalink to &quot;Threading and Parallelism {#Threading-and-Parallelism}&quot;">​</a></h2><p>There are three kinds of parallelism in the codebase:</p><h3 id="Explicit-Julia-threading" tabindex="-1">Explicit Julia threading <a class="header-anchor" href="#Explicit-Julia-threading" aria-label="Permalink to &quot;Explicit Julia threading {#Explicit-Julia-threading}&quot;">​</a></h3><p>Used in several CPU paths with <code>Threads.@threads</code>, especially:</p><ul><li><p>direct velocity loops in <code>src/velocity/common.jl</code></p></li><li><p>diagnostics pair loops in <code>src/diagnostics/</code></p></li></ul><h3 id="KA-managed-CPU-parallelism" tabindex="-1">KA-managed CPU parallelism <a class="header-anchor" href="#KA-managed-CPU-parallelism" aria-label="Permalink to &quot;KA-managed CPU parallelism {#KA-managed-CPU-parallelism}&quot;">​</a></h3><p>When KA kernels run on <code>CPU()</code>, they use <code>KernelAbstractions.CPU()</code>. That is a separate execution path from the explicit threaded loops above.</p><h3 id="GPU-parallelism" tabindex="-1">GPU parallelism <a class="header-anchor" href="#GPU-parallelism" aria-label="Permalink to &quot;GPU parallelism {#GPU-parallelism}&quot;">​</a></h3><p>When the CUDA extension is loaded and <code>dev=GPU()</code>, the active contour state is a device-resident <code>DeviceContourState</code>. Velocity, timestepping, surgery, and diagnostics read that state directly, for single-layer Euler, QG, SQG, and beta-plane QG as well as multi-layer QG. CPU contour reconstruction is reserved for explicit output boundaries such as <code>materialize_contours</code>, JLD2 snapshots, Makie animation frames, and interactive inspection.</p><p>Single-point velocity probes upload one target and copy back the two result scalars; scalar surgery counts and diagnostic reductions may likewise cross for control flow or return values. The OrdinaryDiffEq bridge uses a CPU vector state and therefore rejects GPU problems rather than falling back.</p><h2 id="Read-This-File-If..." tabindex="-1">Read This File If... <a class="header-anchor" href="#Read-This-File-If..." aria-label="Permalink to &quot;Read This File If... {#Read-This-File-If...}&quot;">​</a></h2><ul><li><p>“I want to understand how a user call becomes a simulation”: <code>src/core/problem_factory.jl</code>, then <code>src/core/evolution.jl</code></p></li><li><p>“I want to understand velocity dispatch”: <code>src/velocity/common.jl</code></p></li><li><p>“I want to understand beta-plane QG”: <code>src/core/beta_plane_types.jl</code>, then <code>src/beta_plane.jl</code></p></li><li><p>“I want to understand periodic domains”: <code>src/velocity/periodic/cache.jl</code>, then <code>src/velocity/periodic/single_layer.jl</code></p></li><li><p>“I want to understand acceleration”: <code>src/accel/ka/packing.jl</code>, then <code>src/accel/ka/kernels.jl</code></p></li><li><p>“I want to understand GPU / KA code”: <code>src/accel/ka/</code> (<code>packing.jl</code> → <code>kernels.jl</code> → <code>velocity.jl</code>), then <code>src/accel/ka/surgery/</code> (<code>types.jl</code> → <code>driver.jl</code>, then the stage you need)</p></li><li><p>“I want to understand surgery”: <code>src/core/surgery.jl</code>, then <code>src/accel/ka/surgery/driver.jl</code> for the device pass</p></li></ul><h2 id="Typical-Change-Map" tabindex="-1">Typical Change Map <a class="header-anchor" href="#Typical-Change-Map" aria-label="Permalink to &quot;Typical Change Map {#Typical-Change-Map}&quot;">​</a></h2><ul><li><p>Add a new kernel: type in <code>src/core/kernel_types.jl</code>, direct segment logic, then diagnostics/tests</p></li><li><p>Change beta-plane QG: <code>src/core/beta_plane_types.jl</code> (kernel type and the shared sawtooth formula), <code>src/beta_plane.jl</code> (CPU composition), <code>src/core/contours.jl</code> (<code>beta_staircase</code>), and <code>src/accel/ka/velocity.jl</code> for the device path. The sawtooth jet lives in <code>_beta_sawtooth_u</code> so the CPU evaluator and the KA kernel cannot drift apart — change it there, not in either caller.</p></li><li><p>Change default user construction: <code>src/core/problem_factory.jl</code></p></li><li><p>Change time stepping: <code>src/core/evolution.jl</code></p></li><li><p>Change contour packing / flat buffer logic: <code>src/core/evolution_buffers.jl</code></p></li><li><p>Change public velocity selection policy: <code>src/velocity/common.jl</code></p></li><li><p>Change GPU / KA direct kernels: <code>src/accel/ka/kernels.jl</code> (math), <code>src/accel/ka/velocity.jl</code> (dispatch)</p></li></ul><h2 id="Beginner-Advice" tabindex="-1">Beginner Advice <a class="header-anchor" href="#Beginner-Advice" aria-label="Permalink to &quot;Beginner Advice {#Beginner-Advice}&quot;">​</a></h2><p>Do not start by reading the whole package top-to-bottom.</p><p>A better sequence is:</p><ol><li><p>create a tiny <code>Problem</code></p></li><li><p>follow <code>Problem(...)</code></p></li><li><p>follow <code>evolve!</code></p></li><li><p>follow <code>velocity!</code></p></li><li><p>only then read acceleration internals if needed</p></li></ol><p>That gives the right mental model before the performance layers add complexity.</p>`,73)])])}const m=a(n,[["render",o]]);export{u as __pageData,m as default};
